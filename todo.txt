1. Figure out how to figure out when a block expression ends a plain 
   block without having awkward stuff like 
```tara
fn foo(b: bool) ->
	match b ->
		case true -> print("foo");
		case false -> print("bar");
	;
```
2. Handle unexpected end of stream instead of just panicking when 
   `stack.pop()` fails